<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.finchild.hoppateam.sda4.finchild">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:hardwareAccelerated="false"
        android:largeHeap="true"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".login.LoginActivity" />
        <activity android:name=".Control" />
        <activity android:name=".ChildSpendLimit" />
        <activity android:name=".AccountControl" />
        <activity android:name=".Notifications" />
        <activity android:name=".Autofill" />
        <activity android:name=".AllowedMarkets"/>
        <activity android:name=".HomeActivity" />
        <activity android:name=".Settings" />
        <activity android:name=".AddChildAcount" />
        <activity android:name=".AddParentAccount" />
    </application>

</manifest>
